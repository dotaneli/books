<div class="books new">
  <div class="row">
    <%= error_messages_for(@book) %>
  </div>
  <h2>Create Book</h2>


    <%= form_for(:book, :url => {:action => 'create'}, :html => { :role => "form", :class => "form-horizontal" }) do |f| %>

      <!-- Main form -->
      <div class="row">
        <%= render(:partial => "form", :locals => {:f => f}) %>
      </div>

      <!-- Submit button line -->
      <div class="row">
        <%= render(:partial => "submitbtn", :locals => {:f => f, :cancel_action => 'index', :submit_text => "Create Book"}) %>
      </div>
    <% end %>

</div>
